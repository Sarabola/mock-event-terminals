services:
  mock-terminal:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
      - ./images:/app/images
    network_mode: host
    stdin_open: true
    tty: true
